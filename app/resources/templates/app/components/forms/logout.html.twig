<form id="{{ id }}" method="post" action="{{ path(action) }}">
  <div class="form-group">
    <button type="submit" class="btn btn-danger" form="logout" title="Quit">{{ btnLabel }}</button>
  </div>
</form>
